# Enable verbose logging
sonar.verbose=true

# For SonarCloud
sonar.organization=sam01-key

# Project identifiers
sonar.projectKey=sam01-key-saidemytrend
sonar.projectName=saidemytrend

# Encoding and language
sonar.sourceEncoding=UTF-8
sonar.language=java   # ✅ Optional but valid; often deprecated in newer SonarQube versions

# Source and binary paths
sonar.sources=src
sonar.java.binaries=target/classes

# JaCoCo code coverage (fix typo in 'target')
sonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml

